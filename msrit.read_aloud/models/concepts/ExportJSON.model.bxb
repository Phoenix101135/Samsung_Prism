structure  (ExportJSON) {
  description ("Converts Utterance to JSON")
  property (uri) {
    type (ShareViaUri)
    min (Required) max (One)
  }
  property (json){
    type (Json)
    min(Required) max(One)
  }
  property (message){
    type (Message)
    min(Required) max(One)
  }
}